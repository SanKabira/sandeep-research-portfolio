/**
 * about-3d.js
 * Specific interactions for the About section 3D scene (DNA Helix)
 */

document.addEventListener('DOMContentLoaded', () => {
    // Logic specific to the About section scene could go here.
    // Ensure the element in markdown has correct data attributes
    const aboutScene = document.querySelector('[data-spline-scene="about-main"]');
    if (aboutScene) {
        // Example: Add specific event listeners if needed beyond common.js
    }
});
